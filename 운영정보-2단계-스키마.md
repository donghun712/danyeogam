# 운영정보/시설정보 — 2단계 스키마 제안

1단계 조사 결과(2026-09-10, 관광지/문화시설 각 8건)를 반영한 응답 스키마 제안입니다.
확정되면 3단계(구현)로 넘어갑니다.

## 설계 원칙 (1단계 보고의 처리 의견을 그대로 반영)

- 빈 문자열/공백은 `null`로 변환
- 원문 문자열은 항상 보존 (가공된 값만 주지 않음)
- "가능"/"불가" 같은 상태로 단순화하더라도 원문을 같이 준다
- `parkingfee`가 있다고 주차 가능으로 임의 판정하지 않는다
- 휠체어(무장애)는 `detailIntro2`에 필드 자체가 없으므로 **이번 범위에서 제외**한다.
  별도의 "무장애 관광정보" API가 필요한데, 이건 규모가 다른 작업이라 지금은 안 한다 —
  화면에 휠체어 아이콘 자체를 아직 넣지 않는 것으로 프론트에서 처리한다.

## 제안 스키마

```java
// 운영시간/휴무일 — 원문 그대로, 타입(관광지/문화시설)에 따라 usetime/usetimeculture 등을
// 여기서 하나로 정규화해서 내려준다. 프론트는 어떤 TourAPI 필드였는지 몰라도 된다.
record OperatingInfoResponse(
    String hours,       // 예: "상시 개방", "09:00~18:00" — 원문 그대로, 없으면 null
    String closedDays   // 예: "연중무휴" — 원문 그대로, 없으면 null
) {}

// 시설 하나에 대한 상태. status는 백엔드가 원문에서 가볍게 분류하되(예: "가능"으로
// 시작하면 AVAILABLE, "불가"/"불가능"이면 UNAVAILABLE, 그 외/빈 값이면 UNKNOWN),
// note에 원문을 그대로 남겨서 "가능요금 (무료)" 같은 세부 내용이 안 사라지게 한다.
record FacilityStatusResponse(
    String status,  // "AVAILABLE" | "UNAVAILABLE" | "UNKNOWN"
    String note     // 원문 문자열, 없으면 null
) {}

record FacilityInfoResponse(
    FacilityStatusResponse parking,
    String parkingFeeNote,           // 문화시설의 parkingfee 원문. 해당 없으면 null
    FacilityStatusResponse strollerRental,
    FacilityStatusResponse petAllowed
    // 휠체어 없음 — 위 설계 원칙 참고
) {}
```

`TouristSpotDetailResponse`에 두 필드 추가:

```java
OperatingInfoResponse operatingInfo,  // 없으면 null
FacilityInfoResponse facilityInfo     // 없으면 null
```

## 타입별 필드 매핑 (내부 구현 참고용)

| 응답 필드 | 관광지(12) | 문화시설(14) |
|---|---|---|
| `operatingInfo.hours` | `usetime` | `usetimeculture` |
| `operatingInfo.closedDays` | `restdate` | `restdateculture` |
| `facilityInfo.parking` | `parking` | `parkingculture` |
| `facilityInfo.parkingFeeNote` | (없음, null) | `parkingfee` |
| `facilityInfo.strollerRental` | `chkbabycarriage` | `chkbabycarriageculture` |
| `facilityInfo.petAllowed` | `chkpet` | `chkpetculture` |

`status` 분류 예시(참고용, 정확한 임계값은 실제 값 보면서 조정 가능):
- 원문이 "가능"으로 시작 → `AVAILABLE`
- 원문이 "불가능" 또는 "불가"로 시작 → `UNAVAILABLE`
- 그 외(빈 값, "없음", 애매한 값) → `UNKNOWN`

## 확인 필요

- 위 분류 임계값(특히 "가능요금 (무료)"처럼 접두어는 "가능"이지만 유료인 경우 `AVAILABLE`로
  분류하고 `note`에 세부 내용을 남기는 게 맞는지) — 애매하면 그냥 `UNKNOWN` + `note` 원문
  유지도 괜찮습니다. 프론트는 `UNKNOWN`이면 아이콘 대신 원문 텍스트만 보여주면 되니까요.

이 스키마로 괜찮으면 3단계(구현) 프롬프트 바로 만들어드릴게요.
