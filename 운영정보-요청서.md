# 백엔드 요청서 — 운영시간/휴무일/시설정보 추가 검토

## 배경

화면시안 "03 관광지 상세 페이지"에는 은영시간(09:00-18:00), 휴무일(연중무휴), 시설정보
(주차장/유모차/반려동물/휠체어 아이콘)가 있는데, 현재 `GET /api/v1/tourist-spots/{id}` 응답에는
이 정보가 없습니다.

## 원인 확인 결과 (이미 확인함)

`backend/docs/tourapi-field-mapping.md`를 확인했습니다. 현재 백엔드가 TourAPI에서 실제로
호출하는 기능은 4개뿐입니다.

| 기능 | Operation |
|---|---|
| 광역·시군구 법정동 코드 | `ldongCode2` |
| 지역 기반 목록 | `areaBasedList2` |
| 관광지 공통 상세 | `detailCommon2` |
| 관광지 이미지 | `detailImage2` |

상세 정보는 `detailCommon2`(공통 정보: 제목/주소/좌표/대표사진/전화번호/개요/홈페이지)만
호출하고 있습니다. **운영시간·휴무일·시설정보는 TourAPI의 다른 API인 `detailIntro2`(소개정보)에
있는데, 이 API를 아직 호출하지 않고 있습니다.** 즉 데이터가 없는 게 아니라 아직 안 가져온
상태입니다.

## ⚠️ 이번 작업은 "확인 → 보고 → 구현" 순서로 진행해주세요

이 작업은 `regionCode` 때와 다르게, 정확한 필드명과 값 형식을 실제로 확인하기 전에는
구현부터 들어가면 안 됩니다. 이유는:

1. **`detailIntro2`는 관광지 타입(`contentTypeId`)마다 필드 구성이 다릅니다.** 현재 스탬프
   대상은 관광지(`12`)와 문화시설(`14`) 두 타입인데, 필드명이 타입마다 다를 가능성이 큽니다
   (TourAPI는 보통 타입별로 접미사가 다른 패턴을 씁니다. 예상이지 확정은 아닙니다 — 실제 응답을
   보고 확인해주세요).
2. **값 형식이 boolean이 아닐 가능성이 높습니다.** `주차 가능 여부` 같은 필드가 `"가능"`,
   `"불가"`, 빈 문자열, 또는 다른 자유 텍스트로 올 수 있습니다. 이걸 프론트에 어떤 형태로
   내려줄지(boolean / enum / 원문 문자열)는 실제 값을 보고 정해야 합니다.
3. **모든 관광지에 이 정보가 채워져 있지 않을 수 있습니다.** 값이 없으면 없는 대로
   `null`을 내려주시고, 프론트나 백엔드나 임의의 기본값(예: "운영시간 정보 없음"이 아니라
   "09:00-18:00" 같은 걸 지어내는 것)을 채우면 안 됩니다.

## 요청 순서

### 1단계 — 실제 응답 확인 (구현 전)

관광지(`12`)와 문화시설(`14`) 각각 실제 `contentId` 하나씩 골라서 TourAPI의
`detailIntro2`를 직접 호출해보고, 아래를 정리해서 알려주세요.

- 두 타입 각각의 실제 필드명 목록 (운영시간/휴무일/주차/유모차/반려동물/휠체어 관련)
- 각 필드의 실제 값 예시와 형식(문자열인지, 특정 값 집합인지)
- 값이 비어있는 경우가 얼마나 되는지 대략적인 감(전수 조사까지는 필요 없음)

### 2단계 — 응답 스키마 제안 및 확정

1단계 결과를 보고, `TouristSpotDetailResponse`에 추가할 필드 구조를 제안해주세요. 예를 들면:

```java
// 예시일 뿐, 실제 필드명/타입은 1단계 결과에 따라 다를 수 있음
record OperatingInfoResponse(
    String hours,       // 운영시간 원문 (없으면 null)
    String closedDays   // 휴무일 원문 (없으면 null)
) {}

record FacilityInfoResponse(
    Boolean parkingAvailable,     // 확인 안 되면 null (false 아님)
    Boolean strollerRentAvailable,
    Boolean petAllowed,
    Boolean wheelchairAccessible
) {}
```

`Boolean`은 `true`/`false`/`null`(정보 없음) 3가지 상태를 표현할 수 있어야 합니다 —
"정보 없음"을 "불가능"으로 임의 해석하면 안 됩니다.

### 3단계 — 구현

스키마가 확정된 뒤에만 진행해주세요.

- `detailIntro2` 호출 추가, 응답 매핑
- 기존 `TouristSpotDetailResponse` 필드는 이름/타입/순서 그대로 유지
- Swagger·`docs/frontend-read-api.md` 갱신
- `docs/tourapi-field-mapping.md`에 새 매핑 표 추가 (기존 문서 형식과 동일하게)

## 완료 기준

- [ ] 1단계 결과 보고 (필드명, 값 예시, 타입별 차이)
- [ ] 2단계 스키마 확정
- [ ] 값이 없는 경우 `null` 처리 확인 (임의 기본값 없음)
- [ ] 기존 필드 변경 없음
- [ ] Swagger/문서 갱신

## 참고 — 이번 작업과 무관 (건드리지 않음)

regionCode, STAMP_TARGET 카테고리 필터링, GPS 운영값 — 전부 이번 요청과 별개입니다.
